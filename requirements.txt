# gogs_python 프로젝트 의존성
#
# 실행 의존성 (Runtime Dependencies):
# - Python 3.10 이상 (표준 라이브러리만 사용)
#
# 개발 의존성은 requirements-dev.txt 참조

# 이 파일은 프로덕션 환경에서 사용되며, 런타임에 필요한 패키지가 없습니다.
# gogs_python은 Python 표준 라이브러리만을 사용합니다.
